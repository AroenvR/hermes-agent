# Vault TODO's

- [ ] [setup] Register the Hermes-native nightly cron job for `wikilinks-vault-maintenance` at `0 3 * * *`.
  - Load skills: `wikilinks-vault`, `wikilinks-vault-maintenance`.
  - Vault path: `~/.hermes/vault`.
  - Use Hermes-native cron, not system crontab.
  - Create the initial commit for the vault.
  - After registration is verified, remove this TODO item.
